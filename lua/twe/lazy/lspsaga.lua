-- LSPSaga is actually really dope, but tends to not play nicely without really forcing it.
return {
  -- "nvimdev/lspsaga.nvim",
  -- config = function()
  --   require('lspsaga').setup({
  --     ui = { 
  --       code_action = '_'
  --     }
  --   })
  -- end,
  -- dependencies = {
  --   'nvim-treesitter/nvim-treesitter',
  --   'nvim-tree/nvim-web-devicons'
  -- }
  --
}
